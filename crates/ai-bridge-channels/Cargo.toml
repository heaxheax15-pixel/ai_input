[package]
name = "ai-bridge-channels"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
nix = { version = "0.29", features = ["socket", "uio", "user"] }

[dev-dependencies]
tempfile = "3.0"
