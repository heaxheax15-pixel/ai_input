[package]
name = "ai-bridge-gatekeeper"
version = "0.1.0"
edition = "2021"

[dependencies]
ai-bridge-protocol = { path = "../ai-bridge-protocol" }
regex = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
