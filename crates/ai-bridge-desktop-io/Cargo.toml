[package]
name = "ai-bridge-desktop-io"
version = "0.1.0"
edition = "2021"

[dependencies]
ai-bridge-protocol = { path = "../ai-bridge-protocol" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rusqlite = { version = "0.31", features = ["bundled"] }
url = "2.5"

[dev-dependencies]
tempfile = "3.0"
